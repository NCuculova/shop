spring.datasource.url=jdbc:mysql://localhost/shop?useUnicode=true&characterEncoding=UTF-8
spring.datasource.username=root
spring.datasource.password=admin
spring.datasource.driver-class-name=com.mysql.jdbc.Driver

spring.jpa.hibernate.ddl-auto: update

# Email (MailProperties) Gmail for dev purposes

mail.active=true
mail.host=smtp.gmail.com
mail.port=587
mail.username=nadica.cuculova@gmail.com
mail.password=
mail.protocol=smtp
mail.tls=true
mail.auth=true
# Non partners from email address
mail.from=noreply@e-shop.com

# Specify the DirectoryProvider to use (the Lucene Directory)
spring.jpa.properties.hibernate.search.default.directory_provider=filesystem

# Using the filesystem DirectoryProvider you also have to specify the default
# base directory for all indexes (make sure that the application have write
# permissions on such directory)
spring.jpa.properties.hibernate.search.default.indexBase=index

security.basic.enabled=false